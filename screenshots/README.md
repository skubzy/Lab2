# Screenshot Placeholders

Add the following screenshots here before submission:

- GitHub Actions pipeline run
- prediction output from `artifacts/sample_predictions.png`

These files are intentionally not committed by default because they depend on running the workflow and the model locally or in CI.
